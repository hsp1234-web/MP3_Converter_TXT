# 專案總覽 (Project Overview)

這份文件旨在提供「鳳凰專案」的完整技術細節，包括專案的架構、各個核心檔案的功能，以及所使用的技術棧。

---

## 核心理念

本專案是一個模組化、事件驅動的錄音轉寫系統。其設計目標是：
- **高擴展性**: 能夠輕易地增加新的功能模組 (例如，不同的 AI 模型、新的輸出格式)。
- **高可靠性**: 系統的每個部分都能獨立運作和測試，並有完整的日誌記錄。
- **易於部署**: 提供多種部署方式，從適合所有人的 Google Colab 一鍵部署，到適合開發者的本地腳本。

---

## 專案結構與核心檔案說明

以下是專案根目錄下主要檔案和資料夾的功能詳述：

### 📄 文件 (Documentation)

- **`README.md`**: **專案入口**。提供專案的快速上手指南，是新使用者的第一站。它解釋了專案的用途，並提供了最簡單的啟動方式。
- **`COLAB_USAGE.md`**: **Colab 使用手冊**。為非技術背景的使用者提供鉅細靡遺的圖文教學，解釋如何在 Google Colab 環境中啟動並使用本服務。
- **`PROJECT_OVERVIEW.md`**: **技術總覽 (本文件)**。為開發者提供專案的深入技術細節、架構和設計理念。

### 🚀 啟動腳本 (Launch Scripts)

- **`colab_deployment_script.py`**: **Colab 部署指揮中心**。這是一個獨立的 Python 腳本，專為在 Google Colab 環境中執行而設計。它封裝了所有必要的步驟：下載程式碼、設定環境、安裝依賴、啟動後端服務，並透過 `google.colab.kernel.proxyPort` 生成一個公開的網頁連結供使用者存取。
- **`start.sh`**: **本地一鍵啟動腳本 (Linux/macOS)**。這是一個 Bash 腳本，用於在本機開發環境中快速啟動專案。它會自動偵測環境、安裝依賴，並呼叫 `universal_launcher.py` 來啟動服務。
- **`universal_launcher.py`**: **通用啟動器**。這是一個 Python 腳本，作為 `start.sh` 的底層實作。它負責處理更複雜的啟動邏輯，例如檢查 Python 版本、管理虛擬環境、並協調 `commander_console.py` 來執行具體任務。
- **`commander_console.py`**: **萬能鑰匙 (專案命令列介面)**。這是整個專案所有自動化操作的核心。它使用 `fire` 函式庫將函式轉換為 CLI 指令。所有 CI/CD、測試、安裝、啟動等操作，最終都會透過這個檔案來執行。
  - `install-deps`: 安裝專案所有依賴。
  - `run-tests`: 執行所有自動化測試。
  - `run-server`: 啟動後端網頁伺服器。
  - `...` 更多底層指令。

### ⚙️ 設定檔 (Configuration)

- **`pyproject.toml`**: **Python 專案定義檔**。遵循 PEP 621 標準，定義了專案的名稱、版本、依賴套件 (使用 [Poetry](https://python-poetry.org/))、以及各種開發工具 (如 Ruff, Pytest) 的設定。
- **`poetry.lock`**: **依賴版本鎖定檔**。由 Poetry 自動產生，確保所有開發者和伺服器上安裝的依賴套件版本完全一致，避免「在我電腦上可以跑」的問題。
- **`requirements.txt`**: **Colab 依賴檔**。為了簡化在 Colab 上的安裝流程而產生，內容來自 `poetry.lock`。
- **`logging_config.yaml`**: **日誌系統設定檔**。定義了日誌的格式、輸出位置 (終端機和檔案)、以及不同模組的日誌層級。

### src - 應用程式原始碼

這是專案的核心商業邏輯所在。

- **`main.py`**: **Web 服務入口**。使用 [FastAPI](https://fastapi.tiangolo.com/) 框架建立。它定義了所有的 API 端點 (Endpoints)，例如檔案上傳 (`/upload/`)、狀態查詢 (`/status/`) 等，並負責接收前端請求、驗證參數，然後將任務放入佇列。
- **`transcriber_worker.py`**: **轉寫背景任務處理器**。這是一個獨立的程序，它會不斷地從任務佇列中取出待處理的錄音檔，使用 [Whisper](https://github.com/openai/whisper) 模型進行語音轉文字，並在完成後更新任務狀態。
- **`queues.py`**: **任務佇列模組**。定義了系統中不同模組間溝通用的佇列，例如 `AUDIO_QUEUE` (待處理音檔佇列) 和 `RESULT_QUEUE` (轉寫結果佇列)。
- **`core/`**: **核心共用模組資料夾**。
  - `config.py`: 應用程式層級的設定，例如 AI 模型的預設大小。
  - `hardware.py`: 硬體偵測模組，用於判斷當前環境是否有可用的 GPU。
  - `log_config.py`: 日誌設定的載入器。

### 🧪 tests - 自動化測試

- **`test_*.py`**: **測試案例檔案**。使用 [Pytest](https://docs.pytest.org/) 框架編寫。我們對專案的各個層面進行測試，確保其穩定可靠。
  - `test_capabilities.py`: **功能契約測試**。這個測試會自動讀取 `README.md` 中的命令，並實際執行它們，確保文件中的教學永遠是有效且可運作的。
  - `test_e2e_flow.py`: **端對端流程測試**。模擬一個完整的用戶操作流程：上傳檔案 -> 查詢狀態 -> 確認完成 -> 下載結果，以驗證整個系統的整合運作是否正常。

### 📂 其他資料夾

- **`static/`**: 存放前端靜態檔案，如 `index.html` 和 CSS/JavaScript。
- **`assets/`**: 存放專案相關的非程式資源，例如圖片、樣式表。
- **`IN/`, `uploads/`**: 預設的輸入和上傳資料夾。

---

## 🛠️ 技術棧 (Tech Stack)

### 後端 (Backend)

- **程式語言**: **Python 3.10+**
- **Web 框架**: **FastAPI** - 用於建立高效能的非同步 API。
- **AI 語音辨識**: **OpenAI Whisper** - 用於將語音轉換為文字。
- **依賴管理**: **Poetry** - 用於管理專案的 Python 套件依賴。
- **背景任務**: 使用 Python 內建的 `multiprocessing` 和 `queue` 模組來實現非同步的任務處理。

### 前端 (Frontend)

- **HTML5 / CSS3 / JavaScript**: 使用最基礎的網頁技術，確保最大的相容性。
- **無前端框架**: 目前的前端頁面非常簡單，因此沒有使用任何複雜的前端框架 (如 React, Vue)。

### 開發與測試 (Dev & Test)

- **測試框架**: **Pytest** - 用於編寫和執行單元測試、整合測試與端對端測試。
- **程式碼風格檢查**: **Ruff** - 一個極速的 Python Linter，用於確保程式碼風格一致且無潛在錯誤。
- **命令列介面**: **Python Fire** - 一個能自動將 Python 函式轉換為 CLI 的工具。
- **部署**: **Bash Script**, **Google Colab**。

---

## 系統架構圖 (簡易版)

```
+-----------------+      +-----------------+      +----------------------+
|   使用者 (UI)   | <--> |  FastAPI (Web)  | <--> |  任務佇列 (Queue)  |
+-----------------+      +-----------------+      +----------------------+
      ^                      | (main.py)         (queues.py) |
      |                      |                             |
      | (上傳/查詢)          | (放入待處理音檔)            | (取出待處理音檔)
      |                      V                             V
      |               +-------------------------------------------------+
      +---------------|              轉寫背景處理器 (Worker)            |
                      | (transcriber_worker.py, using Whisper AI model) |
                      +-------------------------------------------------+
```
