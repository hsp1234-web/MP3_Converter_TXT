# 鳳凰專案 (Phoenix Project) 技術分析報告

## 1. 總體概述

鳳凰專案是一個基於 Python 的模組化、可擴展的錄音轉寫服務。其核心功能是接收使用者上傳的音訊檔案，並在背景執行轉寫任務，最終提供文字結果。系統設計良好，具備清晰的結構和自我驗證能力。

## 2. 技術棧

- **後端框架**: `FastAPI` - 一個現代、高效能的 Python Web 框架，用於建立 API。
- **Web 伺服器**: `Uvicorn` - 一個 ASGI 伺服器，用於運行 FastAPI 應用程式。
- **音訊轉寫**: `faster-whisper` - `Whisper` 模型的 CTranslate2 實現，提供高效的音訊轉寫能力。
- **機器學習函式庫**: `torch` - `faster-whisper` 的依賴，用於深度學習任務。
- **資料庫**: `SQLite` - 一個輕量級的伺服器レス資料庫，用於儲存任務狀態和結果。
- **命令列介面**: `click` - 用於建立使用者友好的命令列介面 (`commander_console.py`)。
- **非同步處理**: `asyncio` 和 `aiofiles` - 用於處理非同步 I/O 操作，例如檔案上傳。
- **多行程管理**: `multiprocessing` - 用於同時運行 API 伺服器和背景轉寫工作程序。
- **依賴管理**: `uv` 和 `poetry` - `uv` 用於快速安裝和管理 Python 依賴，`poetry` 用於專案打包和依賴定義。
- **測試框架**: `pytest` - 用於執行自動化測試。

## 3. 檔案詳細功能

| 檔案路徑 | 功能說明 |
| :--- | :--- |
| `commander_console.py` | **萬能鑰匙**。整個應用程式的統一入口點。負責啟動服務、安裝依賴、執行測試等所有核心操作。 |
| `pyproject.toml` | **專案定義檔**。定義專案的元數據、依賴、打包設定等。 |
| `uv.lock` / `poetry.lock` | **依賴鎖定檔**。確保在不同環境中安裝完全相同的依賴版本，保證環境的可重複性。 |
| `src/main.py` | **API 伺服器進入點**。使用 FastAPI 建立 Web 服務，提供檔案上傳、狀態查詢等 API 端點。 |
| `src/transcriber_worker.py` | **真實轉寫工人**。在背景執行的工作程序，負責從資料庫中提取待處理的轉寫任務，並使用 `faster-whisper` 模型進行轉寫。 |
| `src/mock_worker.py` | **模擬轉寫工人**。用於測試環境的模擬工作程序，它不會真正執行轉寫，只會模擬任務的處理流程。 |
| `src/core/__init__.py` | **核心模組**。整合設定、日誌和資料庫初始化等核心功能。 |
| `src/core/hardware.py` | **硬體檢測模組**。用於檢測系統的硬體配置（例如是否有 NVIDIA GPU），以選擇最佳的轉寫設定。 |
| `src/queues.py` | **佇列定義**。定義了用於在不同行程間通訊的佇列。 |
| `static/index.html` | **前端介面**。一個簡單的 HTML 頁面，提供檔案上傳的介面。 |
| `assets/style.css` | **前端樣式**。前端介面的 CSS 樣式表。 |
| `tests/` | **測試目錄**。包含所有自動化測試。 |
| `tests/test_capabilities.py` | **功能契約**。確保 `README.md` 中描述的所有指令都是真實可用的。 |
| `tests/test_e2e_flow.py` | **端到端測試**。模擬真實的使用者操作，從上傳檔案到查詢結果，驗證整個系統的流程。 |
| `uploads/` | **上傳檔案目錄**。儲存使用者上傳的音訊檔案。 |
| `IN/` / `MP3/` | **輸入/輸出目錄**。可能是用於其他處理流程的目錄。 |

## 4. 系統運作流程

1.  **啟動服務**: 使用者在命令列中執行 `python commander_console.py run-server`。
2.  **行程啟動**: `commander_console.py` 會使用 `multiprocessing` 啟動兩個主要的子行程：
    - **API 伺服器行程**: 運行 `src/main.py` 中的 FastAPI 應用程式。
    - **轉寫工人行程**: 運行 `src/transcriber_worker.py` 中的背景工作程序。
3.  **檔案上傳**: 使用者透過瀏覽器或其他 HTTP 客戶端，將音訊檔案上傳到 API 伺服器的 `/upload` 端點。
4.  **建立任務**: API 伺服器接收到檔案後，會：
    - 產生一個唯一的任務 ID。
    - 將檔案儲存到 `uploads/` 目錄。
    - 在 SQLite 資料庫的 `transcription_tasks` 表格中插入一筆新的紀錄，狀態為 `pending`。
5.  **背景處理**:
    - **轉寫工人**在背景不斷地輪詢資料庫，尋找狀態為 `pending` 的任務。
    - 當找到新任務時，工人會將其狀態更新為 `processing`。
    - 工人呼叫 `faster-whisper` 模型，對音訊檔案進行轉寫。
6.  **完成任務**:
    - 轉寫成功後，工人會將任務狀態更新為 `completed`，並將轉寫結果的文字儲存到資料庫。
    - 如果轉寫失敗，工人會將狀態更新為 `failed`，並記錄錯誤訊息。
7.  **查詢結果**: 使用者可以隨時透過訪問 `/status/{task_id}` 端點，來查詢任務的目前狀態和轉寫結果。

## 5. 技術債務與潛在問題

1.  **輪詢資料庫**:
    - **問題**: `transcriber_worker.py` 透過不斷地輪詢資料庫來檢查新任務。這會對資料庫造成不必要的負載，且在任務量大時，會有延遲。
    - **建議**: 使用更有效率的通知機制。例如，API 伺服器在建立新任務後，可以透過佇列（如 `multiprocessing.Queue`、`Redis` 或 `RabbitMQ`）直接通知工人有新任務需要處理。

2.  **大型檔案處理**:
    - **問題**: `src/main.py` 中的 `upload_file` 函式會先將整個檔案讀取到記憶體中 (`content = await file.read()`)，然後再寫入磁碟。當上傳非常大的檔案時，這會消耗大量的記憶體，可能導致伺服器崩潰。
    - **建議**: 使用串流（streaming）的方式處理檔案上傳。FastAPI 支援直接將上傳的檔案以串流的方式寫入磁碟，而不需要先將整個檔案讀入記憶體。

3.  **單一工人**:
    - **問題**: 目前的設計只啟動一個轉寫工人。如果同時有多個轉寫任務，它們必須排隊等待，無法平行處理。
    - **建議**: 實現一個工人池（worker pool）。可以啟動多個轉寫工人行程，並讓它們從任務佇列中獲取任務，以實現平行處理。

4.  **SQLite 的限制**:
    - **問題**: SQLite 是一個檔案型資料庫，在高併發的寫入操作下，效能會成為瓶頸。當系統規模擴大時，SQLite 可能無法滿足需求。
    - **建議**: 在生產環境中，可以考慮更換為更強大的資料庫系統，例如 `PostgreSQL` 或 `MySQL`。

5.  **錯誤處理**:
    - **問題**: 目前的錯誤處理比較基本。例如，如果一個任務失敗了，它只會被標記為 `failed`，沒有自動重試的機制。
    - **建議**: 引入更完善的錯誤處理和重試機制。例如，可以設定重試次數和重試間隔，對於某些可恢復的錯誤（如暫時的網路問題），可以自動重試。

## 6. 優化建議

### 針對大型檔案上傳的優化

要解決大型檔案上傳時記憶體佔用過高的問題，可以修改 `src/main.py` 中的 `upload_file` 函式，使用串流的方式處理檔案。

**修改前**:
```python
async with aiofiles.open(filepath, 'wb') as out_file:
    content = await file.read()  # 讀取整個檔案到記憶體
    await out_file.write(content)
```

**修改後 (更優化的方法)**:
```python
async with aiofiles.open(filepath, 'wb') as out_file:
    while content := await file.read(1024 * 1024):  # 每次讀取 1MB
        await out_file.write(content)
```
這樣修改後，無論上傳的檔案有多大，記憶體佔用都只會維持在一個很低的水平，從而避免了伺服器因記憶體耗盡而崩潰的風險。

### 針對後續擴展性的優化

1.  **引入訊息佇列**:
    - 將目前的資料庫輪詢機制，改為使用真正的訊息佇列（如 `Redis` 或 `RabbitMQ`）。
    - API 伺服器在接收到新任務後，將任務訊息發送到佇列。
    - 轉寫工人從佇列中獲取任務。
    - **好處**:
        - **解耦**: API 伺服器和工人之間完全解耦。
        - **可靠性**: 即使工人在處理任務時崩潰，任務訊息仍然保留在佇列中，可以被其他工人處理。
        - **水平擴展**: 可以輕易地增加更多的工人來處理不斷增長的任務量。

2.  **工人池化**:
    - 實現一個可以動態調整工人數量的工人池。
    - 可以根據任務佇列的長度，自動增加或減少工人的數量。

3.  **微服務化**:
    - 當系統變得非常複雜時，可以考慮將其拆分為多個微服務。例如：
        - **上傳服務**: 專門負責處理檔案上傳和任務建立。
        - **轉寫服務**: 專門負責執行轉寫任務。
        - **通知服務**: 專門負責在任務完成後通知使用者。

4.  **容器化與雲原生部署**:
    - 使用 `Docker` 將應用程式容器化。
    - 使用 `Kubernetes` 或類似的容器編排平台來部署和管理應用程式。
    - **好處**:
        - **彈性擴展**: 可以根據負載自動擴展服務的實例數量。
        - **高可用性**: 如果某個實例失敗，平台會自動啟動一個新的實例來取代它。
        - **環境一致性**: 確保在開發、測試和生產環境中使用完全相同的環境。

## 7. 結論

鳳凰專案是一個結構清晰、設計良好的錄音轉寫服務，具有很好的擴展潛力。目前的實現非常適合中小型應用場景。透過解決上述提到的技術債務，並採納優化建議，這個專案可以平滑地擴展，以應對更大規模和更高併發的挑戰。
