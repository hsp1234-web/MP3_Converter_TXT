# 臨時公開網址生成方案指南

本指南旨在為開發與測試提供多種生成臨時公開網址的備援方案。這些方法皆無需註冊，適用於快速、安全的內網穿透需求。

---

## 如何選擇？

- **在 Colab 環境中？** → **方案 1** 是您的首選，最簡單、最直接。
- **需要比 Colab 工作階段更長的網址存活時間（最長 24 小時）？** → **方案 2** 最穩定。
- **在您自己的 Ubuntu 電腦上，且不想安裝任何東西？** → **方案 3** 或 **方案 4** 是最輕量級的選擇。

---

### **方案 1：Google Colab 內建代理（最簡單）**

#### 適用情境：
- 您已經在 Colab 環境中成功執行了鳳凰專案的伺服器。
- 需要一個**臨時、快速**的公開網址，且**完全不想註冊**任何服務。

#### 特點：
- **Colab 原生功能**：透過一行 Python 程式碼即可生成一個臨時的 HTTPS 網址。
- **完全免費**：無需任何第三方工具或費用。
- **拋棄式網址**：網址會隨著 Colab 工作階段的結束而自動失效，非常適合短期測試。

#### 操作：
在您的 Colab 儲存格中，找到生成網址的程式碼片段，它看起來像這樣：
```python
from google.colab.output import eval_js
public_url = eval_js('google.colab.kernel.proxyPort(8000)')
print(f"您的公開網址是：{public_url}")
```

#### 限制：
- **僅限 Colab**：此方法無法在 Colab 以外的任何環境使用。
- **隨機網址**：您無法自訂網址，且網址可能因為 Google 的政策而受到頻寬或速度限制。

---

### **方案 2：Cloudflare Tunnel (TryCloudflare)**

#### 適用情境：
- 您希望網址的存活時間能超過一個 Colab 工作階段（最長可達 24 小時）。
- 您不介意每次啟動時都獲得一個新的隨機網址。

#### 特點：
- **無需註冊**：直接使用 Cloudflare 提供的免登入臨時通道。
- **高度穩定**：由 Cloudflare 維護，提供穩定的 HTTPS 連線。
- **跨平台**：可以在您的 Ubuntu 電腦或 Colab 中透過指令執行。

#### 操作：
1.  **安裝 `cloudflared`**：
    ```bash
    # 在 Colab 或 Ubuntu 中執行
    wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
    !chmod +x cloudflared-linux-amd64
    ```
2.  **啟動通道**：
    假設您的鳳凰專案伺服器正在 `localhost:8000` 運行，請開啟一個新的終端機或 Colab 儲存格，執行以下指令：
    ```bash
    # 將本地的 8000 埠口暴露出去
    ./cloudflared-linux-amd64 tunnel --url http://localhost:8000
    ```
    您會在輸出中看到一個 `https://*.trycloudflare.com` 的網址。

#### 限制：
- **需要手動安裝**：相較於其他方案，多了一個下載的步驟。
- **隨機網址**：無法獲得固定的子網域。

---

### **方案 3：臨時 P2P 穿透 (localhost.run)**

#### 適用情境：
- 您在自己的 Ubuntu 電腦上進行測試。
- 希望使用最輕量級的方法，不想安裝任何額外的工具。

#### 特點：
- **無需安裝**：幾乎所有的 Linux 發行版（包括 Ubuntu）都內建了 SSH 客戶端。
- **操作簡單**：只需一行 SSH 指令即可完成端口轉發。

#### 操作：
假設您的鳳凰專案伺服器正在 `localhost:8000` 運行，請開啟一個新的終端機，執行以下指令：
```bash
# 將本地的 8000 埠口轉發到一個公開的 80 埠口
ssh -R 80:localhost:8000 ssh.localhost.run
```
您會在輸出中看到一個 `https://*.lhr.life` 之類的網址。

#### 限制：
- **穩定性**：P2P 穿透的連線穩定性可能不如 Cloudflare。
- **金鑰要求**：某些類似的服務可能會要求您提供 SSH 公鑰。

---

### **方案 4：bore.pub**

#### 適用情境：
- 與方案 3 類似，適用於本地 Ubuntu 環境的快速測試。
- 作為 `localhost.run` 的一個備援選項。

#### 特點：
- **無需安裝**：透過 `curl` 和 `bash` 的組合指令即可執行。
- **極度簡單**：指令非常簡短，容易記憶。

#### 操作：
假設您的鳳凰專案伺服器正在 `localhost:8000` 運行，請開啟一個新的終端機，執行以下指令：
```bash
# 將本地的 8000 埠口暴露出去
curl -sSL bore.pub | bash -s 8000
```
您會在輸出中看到一個 `https://*.bore.pub` 的網址。

#### 限制：
- **服務可靠性**：這類由社群維護的免費服務，可能隨時會關停。
- **不適合長期使用**：僅建議用於臨時的開發或展示。
